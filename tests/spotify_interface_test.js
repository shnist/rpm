var sinon = require('sinon'),
	chai = require('chai');

describe('spotify/spotify_interface', function () {

	describe('Given I want to login', function () {

		describe('When I attempt to log in with VALID user credentials', function () {

			it('should return with a status of 200', function () {

			});

			it('should return with a successful status message', function () {

			});
		});


		describe('When I attempt to log in with INVALID user credentials', function () {

			it('should return with a status of 401', function () {

			});

			it('should return with an unauthorised status message', function () {

			});
		});
	});	
});
